{
  "name": "calculadora-final",
  "version": "1.0.0",
  "description": "Versión final estilizada de calculadora",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir -p dist && cp index.html style.css script.js favicon.ico dist/",
    "start": "echo 'Este proyecto es estático. Usa build.'"
  },
  "devDependencies": {}
}
