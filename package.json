{
  "name": "calculadora-node",
  "version": "1.0.0",
  "description": "App de calculadora simple con Node.js y Express",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo \"Preparando build...\" && mkdir -p dist && cp -r index.html style.css server.js dist/"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
